# Composer 依赖与锁文件（library 包惯例：不提交 lock）
/vendor/
composer.phar
composer.lock
# 本地开发配置（含 path repository 指向 ../route-forge-common），不提交版本库；
# 本地安装用：COMPOSER=composer.local.json composer update
composer.local.json
composer.local.lock

# PHPUnit
/.phpunit.cache/
.phpunit.result.cache
/coverage/

# Node（package.json 仅作脚本桥接，无前端产物）
/node_modules/

# IDE / 编辑器
/.idea/
/.vscode/
*.swp

# 环境变量与本地配置
.env
.env.*

# 操作系统
.DS_Store
Thumbs.db
desktop.ini
